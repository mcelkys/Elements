Articles.define('atts', Macro => ({
    tag: 'p',
    html: `This is simply a shorthand option of ${Macro.ATTRIBUTES}. It is
    identical to ${Macro.ATTRIBUTES} in every way; it just has a shorter name.`
}));
