Articles.define('on', Macro => ({
    tag: 'p',
    html: `This is simply a shorthand option of ${Macro.LISTENERS}. It is
    identical to ${Macro.LISTENERS} in every way; it just has a shorter name.`
}));
